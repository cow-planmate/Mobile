/**
 * 인증 화면 공용 토큰.
 *
 * 값은 design/planmate-tokens.json의 radius 스케일을 그대로 옮긴 것이다.
 * 화면마다 곡률을 따로 정하면 같은 화면에 8과 14가 섞이므로 여기서만 정한다.
 *
 * src/theme/theme.ts에도 borderRadius가 있으나 그쪽은 커뮤니티·일정 화면이
 * 쓰는 이전 스케일(4/6/8/12/16)이다. 두 스케일을 합치는 일은 이 범위 밖이다.
 */
export const RADIUS = {
  /** 배지, 칩 내부 */
  xs: 6,
  /** 소형 버튼, 태그 */
  sm: 10,
  /** 입력 필드, 기본 버튼 */
  md: 14,
  /** 카드, 모달 */
  lg: 20,
  /** 바텀시트, 다이얼로그 */
  xl: 26,
  /** 아바타, 원형 아이콘 버튼 */
  full: 9999,
} as const;
